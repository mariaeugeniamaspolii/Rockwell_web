import React from "react";
import Form from "react-bootstrap";

const Form = () => {
    return (
        <Form className="d-flex">
            <Input type="text" placeholder="Search.." icon=""></Input>
        </Form>
    )
}

export default Form;